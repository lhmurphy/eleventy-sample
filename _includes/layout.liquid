<!DOCTYPE html>
<html lang = "en">
<body>
    <header>
        <a href="/"> Laura Murphy </a>
    </header>
    <main>
        {% if title %}<h1>{{title}}</h1> {% endif %}
        {%- if dog %}
            <img src="{{dog}}" alt="a dog" />
        {%- endif %}

        {{content}}
    </main>
</body>

 